import React, { Component } from 'react'

function SearchResult(props) {
        return (
            <> 
                {props.name} <span class="second">{props.attr}</span>
            
        </>
        )
        
    }
export default SearchResult